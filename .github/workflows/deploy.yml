name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Smoke test
        run: curl -f https://jckmy-cs-326.vercel.app