name: project_test
description: "A new Flutter project."

# Prevents accidental publishing to pub.dev
publish_to: 'none'

# Versioning
version: 1.0.0+1

# Dart SDK version
environment:
  sdk: ^3.6.2

# Dependencies
dependencies:
  flutter:
    sdk: flutter
  intro_slider: ^4.2.1
  http: 1.3.0



  cupertino_icons: ^1.0.8
  
  # Add carousel_slider for swipe feature if needed
  carousel_slider: ^4.2.1 


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Correctly formatted assets section
  assets:
    - assets/bg.png  # Ensure this file exists inside the assets folder
    - assets/intro2.png
    - assets/intro3.jpg
    - assets/intro_pic1.png
    - assets/sign-up_bg.png
    - assets/Logo.png
    - assets/app_CP.png

